package com.nhnacademy.practice_problems_24;

public class Main {
    public static void main(String[] args) {
        //####문제 24: boolean 타입 필드의 Getter 이름 규칙을 설명하고, 예시를 작성하세요.
        /*
        public class Todo {
           private boolean done;

            // boolean Getter 작성
        }

        정답 : 규칙: boolean 타입은 get 대신 is 로 시작

        Ex)
        private boolean active;    → public boolean isActive()
        private boolean visible;   → public boolean isVisible()
        private boolean completed; → public boolean isCompleted()
         */
    }
}
